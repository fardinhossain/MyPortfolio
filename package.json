{
  "name": "fardin-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build && esbuild server.js --bundle --platform=node --format=cjs --packages=external --outfile=server.bundle.cjs",
    "preview": "vite preview",
    "start": "node server.bundle.cjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dompurify": "^3.4.8",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
